node scripts/check-secrets.mjs
pnpm exec lint-staged

# Public-facing prose. Only the copy this commit adds is judged, and nothing
# below "clean" passes - errors and warnings both block.
node scripts/check-prose.mjs

# Line coverage, per file. Checked here rather than on push because the rule is
# about the diff in front of you: no file this commit touches may lose coverage,
# and a new production file has to clear the bar. Both questions need the staged
# file list, and both should be asked of the person making the change.
node scripts/coverage-ratchet.mjs --staged --advance
